### Objective
In this project sample, we are testing axios features/ functions.

### Dependencies
We have the following dependencies
1. vite for building the app
2. vitest for testing axios setup.
3. jsonplaceholder api for fetching data

### Run the project
- To run the project, use `npx vite`
function displayAge() {
    const age = 30
    console.log(`Age is: ${age}`)
}
displayAge()

// Note: The 'age' variable is isolated within the function scope and does not pollute the global window object